{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs",
    "build": "npm run typecheck",
    "test": "echo \"No backend tests defined\" && exit 0",
    "db:migrate:local": "wrangler d1 migrations apply ot_assistant_dev --local"
  },
  "dependencies": {
    "@clerk/backend": "^1.19.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.87.0"
  }
}
